# =========================================================================
# CalTrack Web - AWS S3 + CloudFront Production Environment Template
# =========================================================================

# Target API Server URL
# Set to relative path /api so CloudFront routes requests to the API Load Balancer,
# preventing Cross-Origin Resource Sharing (CORS) preflight blockages.
VITE_API_URL=/api
